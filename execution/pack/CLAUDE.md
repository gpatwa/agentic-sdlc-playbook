@AGENTS.md

This repo's run guide lives in `AGENTS.md` — the line above imports it
verbatim into this session per Claude Code's own `@path` import convention.
Nothing Claude-Code-specific needs to live in this file separately: the
"Claude Code specifics" section inside `AGENTS.md` already covers it.
