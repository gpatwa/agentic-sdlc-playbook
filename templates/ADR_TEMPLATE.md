# ADR <NNNN>: <the decision, as a short statement>

> Architecture Decision Record. Owned by the Architect; lives at
> `docs/adr/<NNNN>-<slug>.md` in the product repo. One decision per record.
> Never edited after it is accepted — a changed decision gets a new ADR that
> supersedes this one, so the history of *why* survives.

- **Status:** proposed / accepted / superseded by ADR <NNNN>
- **Date:** <YYYY-MM-DD>
- **Slice:** `runs/<slice-id>/` — where the decision was made

## Context

<What forced a decision. The constraint, the requirement, or the failure. Name
the intent's "Done means" lines or Stakes that it serves.>

## Decision

<What was chosen, stated so someone who was not there can apply it.>

## Alternatives considered

<Each real alternative, and the specific reason it lost. "It's popular" and
"we know it" are reasons; say so if they were the reasons.>

## Consequences

<What this makes easier, what it makes harder, and what would make us revisit
it. A decision with no stated downside has not been examined.>
