# Feature Spec — <slice name>

> Owner: UX Researcher Agent
> Status: <draft / ready for design>
> Source PRD: <path>

## Personas in scope

Only the personas affected by this slice. Skip the rest.

- **<Persona>:** <one sentence on goal and context>

## Primary journey (happy path)

Step-by-step. Each step is a concrete user action and a concrete system
response.

1. User <action> → System <response>
2. User <action> → System <response>
3. ...

## Alternate journeys

For each variant, list the divergence point and where it ends:

- **<Variant name>:** Diverges at step <n>. Ends at <state>.

## Edge cases

Each edge case names the trigger and the expected system behaviour:

- <Trigger>: <expected system behaviour>

## States to design

For each screen the journey touches, list the states the designer must
cover:

- **<Screen>:** empty / loading / error / success / <product-specific>

## Accessibility

Concrete behaviours, not "be accessible":

- Keyboard: <focus order, shortcuts>
- Screen reader: <labels, live regions>
- Contrast: <floor>
- Motion: <reduced-motion behaviour>

## Out of scope (explicit)

- <bullet>

## Hand off

Next agent: UI Designer.
Artefacts to produce: UX spec.
